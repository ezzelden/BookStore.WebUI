@model IEnumerable<string>


@foreach (var link in Model)
{
    @Html.RouteLink(link,
    new { controller= "Book", Action= "List",specilization=link, page=1},
    new { @class="btn btn-block btn-default btn-lg " + ( (link == ViewBag.SelectedSpec) ? "btn-primary": "").ToString() })
}